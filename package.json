{
  "name": "homestart-marketing",
  "version": "1.0.0",
  "private": true,
  "description": "HomeStart marketing site (Cloudflare Pages)",
  "scripts": {
    "build": "eleventy && node -e \"require('fs').copyFileSync('_headers','_site/_headers')\"",
    "serve": "eleventy --serve",
    "clean": "rm -rf _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
