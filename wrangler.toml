# Optional: local testing with Functions + KV
# Run: npx wrangler pages dev _site --kv WAITLIST_KV
#
# Production bindings are set in Cloudflare Dashboard → Pages → Settings → Functions.

name = "homestart-web"
compatibility_date = "2024-01-01"
pages_build_output_dir = "_site"

# Uncomment for local KV after: npx wrangler kv namespace create WAITLIST
# [[kv_namespaces]]
# binding = "WAITLIST_KV"
# id = "your_kv_id_here"
