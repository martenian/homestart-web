# Cloudflare Pages + Wrangler: KV binding for waitlist (`/api/waitlist`).
# Also used for local: npx wrangler pages dev _site
#
# If the dashboard binding ever conflicts, prefer one source of truth—usually this file for Git deploys.

name = "homestart-web"
compatibility_date = "2024-01-01"
pages_build_output_dir = "_site"

[[kv_namespaces]]
binding = "WAITLIST_KV"
id = "8e13d57ae74e4885a0eb581d26d2717b"
